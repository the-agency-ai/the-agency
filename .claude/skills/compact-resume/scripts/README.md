# compact-resume — scripts

No custom scripts required — the skill composes existing framework tools (`agency/tools/session-pickup`, `agency/tools/dispatch`, `agency/tools/git-safe`) and skills (`/monitor-dispatches`, `/monitor-ci`, `/changelog-watch`) for monitor re-launch.
