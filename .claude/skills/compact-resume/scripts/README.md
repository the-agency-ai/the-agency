# compact-resume — scripts

No custom scripts required — the skill composes existing framework tools (`claude/tools/session-pickup`, `claude/tools/dispatch`, `claude/tools/git-safe`) and skills (`/monitor-dispatches`, `/monitor-ci`, `/changelog-watch`) for monitor re-launch.
