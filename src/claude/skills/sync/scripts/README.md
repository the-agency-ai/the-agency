# scripts/

Empty by design. `sync` composes `git-captain fetch` + `git merge` + `agency/tools/git-push`. No skill-specific scripts needed.

Bundle structure requires this directory; this README explains the intentional emptiness.
