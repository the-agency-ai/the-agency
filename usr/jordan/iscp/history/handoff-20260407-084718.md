---
type: handoff
agent: the-agency/jordan/iscp
workstream: iscp
date: 2026-04-07
trigger: principal-requested pause
---

## Identity

`the-agency/jordan/iscp` -- ISCP workstream agent. I build and maintain the Inter-Session Communication Protocol: the notification, dispatch, and flag infrastructure that connects all agents.

## Current State

ISCP v1 complete, 174 BATS tests green. Branch `iscp` fast-forwarded to main (zero divergence). Phase 2 plan written and approved by principal.

**In-progress edit:** Migration framework partially written in `agency/tools/lib/_iscp-db`. Two edits applied:
1. Added `_iscp_migrate_v0_to_v1()` and `_iscp_run_migrations()` functions (after line 220, before pragma preamble section)
2. Refactored `iscp_db_init()` to use migration runner for upgrades (existing DB at older version calls `_iscp_run_migrations()` instead of re-running full DDL)

**Tests NOT yet run** on the migration framework changes. The edits were interrupted before verification. First action on restart: run `bats tests/tools/iscp-db.bats` to check regression.

## Phase 2 Plan (approved)

Plan file: `/Users/jdm/.claude/plans/cozy-coalescing-steele.md`

Sequencing: 2.0 → 2.1 → 2.3 → 2.2 → 2.4 → 2.5

| Iter | Deliverable | Status |
|------|-------------|--------|
| 2.0 | Schema migration framework | **IN PROGRESS** — code written, untested |
| 2.1 | Baseline verify (174 tests) | **DONE** — all pass |
| 2.3 | Flag categories | Not started |
| 2.2 | Dispatch authority | Not started |
| 2.4 | Dispatch-on-commit wiring | Not started |
| 2.5 | Health metrics data layer | Not started |

## Unread Dispatches (3)

- #102 escalation from captain: settings.json updated — restart notification
- #101 dispatch from devex: Re: MAR V2 Plan review
- #95 review: MAR V2 Plan ISCP perspective (re-appeared as unread — was resolved in prior session, may be a status issue)

## Flags in Queue (26 items)

Mix of process seeds, friction reports, design decisions, and Anthropic issues. Not urgent — backlog material. 4 are ISCP-relevant (SMS dispatches, captain's log, friction pattern, release notes).

## Key Decisions

- Migration framework design: each `_iscp_migrate_vN_to_vN+1()` emits SQL to stdout; runner wraps in BEGIN/COMMIT + PRAGMA user_version bump
- `ISCP_SCHEMA_VERSION` stays at 1 for iteration 2.0 (framework only); bumps to 2 in iteration 2.3 when flag categories add the first real migration
- `_iscp_migrate_v0_to_v1()` outputs `_iscp_schema_ddl` for testing the framework
- Dispatch authority: captain detected by agent name suffix `/captain` (hardcoded, not config)
- Review-response authority: sender must match `to_agent` of the original review dispatch

## Next Action

1. Process 3 unread dispatches (#102, #101, #95)
2. Run `bats tests/tools/iscp-db.bats` to verify migration framework changes
3. Write migration framework tests (~8 new tests)
4. Complete iteration 2.0, then proceed to 2.3 (flag categories)

## Startup Actions

1. Set dispatch loop: `/loop 5m dispatch check`
2. Process unread dispatches: `dispatch list`
3. Process unread flags: `flag list`
4. Read plan: `/Users/jdm/.claude/plans/cozy-coalescing-steele.md`
5. **FIRST:** Run `bats tests/tools/iscp-db.bats` to verify in-progress migration framework edits
6. Continue from Next Action above
