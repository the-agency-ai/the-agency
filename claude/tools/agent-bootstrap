#!/bin/bash
# agent-bootstrap — RETIRED (D42-R3)
#
# What Problem: This tool performed runtime principal resolution for agent
# registrations at .claude/agents/{A}.md (flat path). It resolved $USER →
# principal → usr/{P}/{A}/CLAUDE-{A}.md and output the content.
#
# Why Retired: D42-R3 moved agent registrations to .claude/agents/{P}/{A}.md
# (principal-scoped). Principal resolution is now structural — the registration
# file directly @imports usr/{P}/{A}/CLAUDE-{A}.md. No runtime resolution needed.
#
# If you're seeing this: your agent registration still calls agent-bootstrap.
# Update it to .claude/agents/{P}/{A}.md with structural @import. See:
# claude/REFERENCE-WORKSTREAM-CONTENT-SPLIT.md
#
# Written: 2026-04-15 (D41-R19, issue #111)
# Retired: 2026-04-16 (D42-R3, issue #121)

echo "DEPRECATED: agent-bootstrap is retired (D42-R3)." >&2
echo "  Agent registrations now live at .claude/agents/{P}/{A}.md" >&2
echo "  with structural @import for principal context." >&2
echo "  See: claude/REFERENCE-WORKSTREAM-CONTENT-SPLIT.md" >&2
exit 0
