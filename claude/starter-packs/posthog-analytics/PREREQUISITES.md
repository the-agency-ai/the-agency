# Prerequisites

## Required Packs

- [x] `node-base` - Must be completed first
- [x] `react-app` - For client-side tracking

## Required Accounts

- [ ] PostHog account (free tier available at https://posthog.com)

## Verification

```bash
# From your project directory
node --version    # v22+
pnpm --version    # 9+
ls src/app/       # Next.js app directory exists
```
